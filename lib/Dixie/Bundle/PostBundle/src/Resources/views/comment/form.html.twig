{#{% import 'form/macros.html.twig' as macros %}#}
{% if is_granted('ROLE_USER') %}
<div class="d-flex mb-3">
    <!-- Avatar -->
    <div class="avatar avatar-xs me-2">
        <a href="#!"><img class="avatar-img rounded-circle" src="{% if  app.user.avatar %}{{ media_thumb_reference( app.user.avatar, 'square_small') }}{% else %}{{ asset('images/avatar.png') }}{% endif %}" alt=""></a>
    </div>

    {{ form_start(form, {'attr': {'class': 'nav nav-item w-100 position-relative'}}) }}
    {% block form_comment %}
        {{ form_widget(form.message, {'attr': {'class': 'form-control pe-5 bg-body-tertiary', 'rows': '1'}}) }}
    {% endblock %}

    <button class="nav-link bg-transparent px-3 position-absolute top-50 end-0 translate-middle-y border-0" type="submit">
        <i class="fas fa-message"></i>
    </button>

    {{ form_rest(form) }}
    {{ form_end(form) }}

    <button type="submit" class="btn btn-primary btn-submit btn-raised ms-2">Post</button>

    <!-- Comment box  -->
{#    <form class="nav nav-item w-100 position-relative">#}
{#        <textarea data-autoresize="" class="form-control pe-5 bg-light" rows="1" placeholder="Add a comment..."></textarea>#}
{#        <button class="nav-link bg-transparent px-3 position-absolute top-50 end-0 translate-middle-y border-0" type="submit">#}
{#            <i class="fas fa-message"></i>#}
{#        </button>#}
{#    </form>#}

{#    <button type="submit" class="btn btn-primary btn-submit btn-raised ms-2">Post</button>#}

</div>
{% else %}
{% endif %}


