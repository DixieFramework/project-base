{% extends _layout %}

<twig:block name="body">
    {% apply markdown_email %}
        # Hey {{ user.username }},

        Welcome to the Devscast community, we are very happy to count you among our members.
        But before going any further, we would like to reassure ourselves that it is you who created the account,
        to do this you are asked to simply confirm your registration.

{#        {%- if _format == 'html' -%}#}
{#            <twig:DashliteEmailQrCode#}
{#                src="{{ qr_code_data_uri(url('auth_register_confirm', {token: user.emailVerificationToken})) }}"#}
{#                title="Scanner le QRCode pour confirmer votre inscription ou utiliser le lien"#}
{#            />#}
{#        {%- endif -%}#}

        This link can be used to confirm your Devscast registration. We never ask for anything else.

        <twig:TalavEmailButton title="Confirm E-Mail" url="{{ url('talav_user_registration_confirm', {token: user.confirmationToken}) }}" :format="_format" />
    {% endapply %}
</twig:block>
